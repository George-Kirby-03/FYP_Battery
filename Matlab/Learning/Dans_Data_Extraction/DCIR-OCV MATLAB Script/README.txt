To run:

- Enter "[capacity, ocv_data, R_charge, R_mixed1, R_mixed2, R_discharge] = getCellChars('FILENAME.csv', true);" into the Command Window in MATLAB - make sure the directory is of the folder with this matlab script in and the data file itself. 
- Change the FILENAME to be which file you want to process